# sapientpb — provenance

Generated SAPIENT **BSI Flex 335 v2.0** Go bindings, **copied verbatim** from the
reference bridge `neuron-rid-bridge/internal/sapientpb/` (module
`github.com/neuron-world/neuron-rid-bridge`, branch `002-rid-to-sapient` @ `1efe8dd`)
on 2026-06-02.

- **Upstream source of truth**: `github.com/dstl/SAPIENT-Proto-Files` → `bsi_flex_335_v2_0`.
  The `.proto` sources are vendored under `proto/sapient_msg/` for provenance + future regen.
- The files are a single self-contained `package sapientpb`; their only imports are the
  protobuf runtime (`google.golang.org/protobuf/*`, `timestamppb`) — no bridge-module
  imports — so they compile **unchanged** in this module (no import rewrite needed).
- Generated by `protoc-gen-go v1.36.11` / `protoc v7.35.0`; matches this module's pinned
  `google.golang.org/protobuf v1.36.11`.

## Regeneration (DEFERRED — not required for Phase 1)

Phase 1 uses the copied `.pb.go` files as-is. When regen is wired against this module, map
every vendored proto to the Go package
`github.com/neuron-sdk/neuron-go-sdk/internal/dapp/sapient/sapientpb` (the upstream protos
carry no `option go_package`, so it must be supplied via `--go_opt=M<rel>=<gopkg>`), e.g.

```
protoc -I proto --go_out=. --go_opt=module=github.com/neuron-sdk/neuron-go-sdk \
  $(for f in $(cd proto && find . -name '*.proto'); do echo --go_opt=M${f#./}=github.com/neuron-sdk/neuron-go-sdk/internal/dapp/sapient/sapientpb; done) \
  $(find proto -name '*.proto')
```
