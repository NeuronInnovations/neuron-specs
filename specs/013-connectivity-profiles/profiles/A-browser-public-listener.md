# Profile A — Browser to Public Listener (pending)

This file is a stub. The normative Profile A definition lives in [`../spec.md`](../spec.md) section "Profile Definitions → Profile A — Browser → Public Listener".

The full per-profile artifact — with expanded runtime-context descriptions, per-binding capability matrices, sample descriptor fixtures, and test-vector references — is produced by `/speckit.plan` for spec 013.

**Normative summary** (see `../spec.md` for the complete requirement set):

- **Profile id**: `a-browser-to-public-listener/1`
- **Runtime contexts**: initiator = browser tab; responder = publicly-reachable listener process
- **Valid transport bindings**: `T-WSS`, `T-WebTransport`
- **Authoritative browser-side FRs**: spec 012

See FR-CP-004 and the Profile A subsection of `../spec.md` for required capabilities, optional capabilities, unsupported assumptions, failure modes, and the conformance statement.
